local LocalizationService = game:GetService("LocalizationService")
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "DongxanhHub | Beta0.1 Menu",
   LoadingTitle = "Loading DongxanhHub...",
   LoadingSubtitle = "by Dong",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "DongHubConfig",
      FileName = "MainHub"
   },
   KeySystem = false
})

local MainTab = Window:CreateTab("FarmLevel", 4483362458)

_G.FarmLevel = false

MainTab:CreateToggle({
   Name = "Farm Level",
   CurrentValue = false,
   Flag = "FarmLevelToggle",
   Callback = function(Value)
      _G.FarmLevel = Value
      
      if _G.FarmLevel then
         spawn(function()
            while _G.FarmLevel do
               task.wait(0.05) 
               
               pcall(function()
                   local localChar = game:GetService("Players").LocalPlayer.Character
                   
                   local args1 = {
                       [1] = localChar,
                       [2] = "Melee"
                   }
                   game:GetService("ReplicatedStorage").Modules.Shared.Event.Remotes.RequestMeleeHitbox:FireServer(unpack(args1))
                   
                   local mobsFolder = workspace:FindFirstChild("Mobs")
                   if mobsFolder then
                       for _, mob in ipairs(mobsFolder:GetChildren()) do
                           local args3 = {
                               [1] = mob,
                               [2] = "Melee"
                           }
                           game:GetService("ReplicatedStorage").Modules.Shared.Event.Remotes.RequestMeleeHitbox:FireServer(unpack(args3))
                       end
                   end
               end)
               
               print("Farm...")
            end
         end)
      end
   end,
})

local QuestSection = MainTab:CreateSection("Quest Auto")

_G.QuestLv1_10 = false
_G.QuestLv10_20 = false
_G.QuestLv20_30 = false

local NpcPosition = CFrame.new(1028.89368, 246.434509, 2067.24268)
local FarmPosition = CFrame.new(1076.27795, 246.434509, 2151.44067)

QuestSection:CreateToggle({
   Name = "QuestLv1-10",
   CurrentValue = false,
   Flag = "Quest1_10",
   Callback = function(Value)
      _G.QuestLv1_10 = Value
      if Value then
         spawn(function()
            while _G.QuestLv1_10 do
               pcall(function()
                   local localPlayer = game.Players.LocalPlayer
                   local character = localPlayer.Character
                   local humanoidRoot = character and character:FindFirstChild("HumanoidRootPart")
                   
                   if humanoidRoot then
                       humanoidRoot.CFrame = NpcPosition
                       task.wait(0.5)
                       
                       local npc = workspace.NPCs:FindFirstChild("S\225\186\163nh") or workspace.NPCs:FindFirstChild("Sánh")
                       local questObj = npc and npc:FindFirstChild("Quests") and npc.Quests:FindFirstChild("Trainer_Marcus")
                       
                       if questObj then
                           local args1 = {
                               [1] = questObj
                           }
                           game:GetService("ReplicatedStorage").Modules.Shared.Event.Remotes.TalkToNPC:FireServer(unpack(args1))
                           
                           task.wait(0.2)
                           
                           local args2 = {
                               [1] = "rookie_training_2"
                           }
                           game:GetService("ReplicatedStorage").Modules.Shared.Event.Remotes.ExitQuest:FireServer(unpack(args2))
                           
                           task.wait(0.2)
                           
                           local args3 = {
                               [1] = "rookie_training_2"
                           }
                           local questResult = game:GetService("ReplicatedStorage").Modules.Shared.Event.Remotes.QuestStart:InvokeServer(unpack(args3))
                           
                           if questResult ~= false then
                               task.wait(0.5)
                               humanoidRoot.CFrame = FarmPosition
                               task.wait(8.5)
                           end
                       end
                   end
               end)
               task.wait(1)
            end
         end)
      end
   end,
})

QuestSection:CreateToggle({
   Name = "QuestLv10-20",
   CurrentValue = false,
   Flag = "Quest10_20",
   Callback = function(Value)
      _G.QuestLv10_20 = Value
      if Value then
         print("Đã bật nhánh QuestLv10-20 (Test)")
      end
   end,
})

QuestSection:CreateToggle({
   Name = "QuestLv20-30",
   CurrentValue = false,
   Flag = "Quest20_30",
   Callback = function(Value)
      _G.QuestLv20_30 = Value
      if Value then
         print("Đã bật nhánh QuestLv20-30 (Test)")
      end
   end,
})

local PlayerTab = Window:CreateTab("Player", 4483345998)

PlayerTab:CreateSlider({
   Name = "Speed",
   Range = {16, 500},
   Increment = 1,
   Suffix = " Speed",
   CurrentValue = 16,
   Flag = "SpeedSlider",
   Callback = function(Value)
      game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
   end,
})

PlayerTab:CreateSlider({
   Name = "Jump",
   Range = {50, 500},
   Increment = 1,
   Suffix = " Power",
   CurrentValue = 50,
   Flag = "JumpSlider",
   Callback = function(Value)
      game.Players.LocalPlayer.Character.Humanoid.JumpPower = Value
   end,
})

local MiscTab = Window:CreateTab("Misc", 4483345998)

MiscTab:CreateButton({
   Name = "Close Menu",
   Callback = function()
      Rayfield:Destroy()
   end,
})

Rayfield:Notify({
   Title = "DongxanhHub On!",
   Content = "",
   Duration = 5,
   Image = 4483345998,
})
